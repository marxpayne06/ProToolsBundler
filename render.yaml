services:
  - type: web
    name: protools-bundler-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false        # You set this manually in Render dashboard
    healthCheckPath: /
